# 2026-05-01 进展

- 用户提供飞书企业自建应用配置：`FEISHU_APP_ID`、`FEISHU_APP_SECRET`、`FEISHU_BITABLE_APP_TOKEN`。
- 已在本地创建 `.env.local`，但该文件被 `.gitignore` 忽略，不应提交到 GitHub。
- 注意：用户把 `FEISHU_BITABLE_APP_TOKEN` 贴成了带 query 的完整片段，脚本已兼容处理，只取 `?` 前的 Base token。
- 运行 `npm run setup:feishu` 时，tenant token 获取成功，但创建数据表返回 `Forbidden`，说明 App ID / Secret 基本可用，当前主要问题是多维表格权限或协作者/数据权限未配置完整。
- 下一步排查：确认飞书开放平台应用已开通并发布 bitable/base 读写权限；确认该企业自建应用已被添加为该多维表格 Base 的可编辑协作者；确认 Base 与应用在同一飞书租户/企业下。
- 用户再次要求运行 `npm run setup:feishu`；脚本仍在创建 `Leads` 数据表时返回 `Forbidden (/open-apis/bitable/v1/apps/ICD6bEPS3a6brpslmV8cUD65nxb/tables)`，进一步确认不是代码语法/环境变量缺失问题，而是飞书 Base 权限、应用协作者、权限范围或应用状态问题。
- 用户反馈企业自建应用无法被添加为已有 Base 的可编辑协作者，因此选择“改成全自动”路线。
- 已改造 `scripts/setup-feishu.mjs`：支持 `FEISHU_SETUP_MODE=create-base`，可自动创建新的飞书多维表格 Base，再创建 Leads、Scholarships、AI Runs 三张表和全部文本字段；`docs/FEISHU_SETUP.md` 已补充该模式。
- 使用 `FEISHU_SETUP_MODE=create-base npm run setup:feishu` 已成功创建新 Base：`AI 奖学金匹配助手 - 顾问工作台`。
- 新飞书配置已写入本地 `.env.local`：`FEISHU_BITABLE_APP_TOKEN=HFD5bJChOaVYl8sWDegcaWMZnIA`，`FEISHU_LEADS_TABLE_ID=tblBRlgBEVL5v1xq`，`FEISHU_SCHOLARSHIPS_TABLE_ID=tblt8OVCvgm2gKXt`，`FEISHU_AI_RUNS_TABLE_ID=tblZFPveSOBPmS6d`。
- 测试 `/api/leads` 时首次出现 `TextFieldConvFail`，已修复：在 `src/lib/feishu/client.ts` 中统一把所有写入飞书的字段转为文本，boolean 转“是/否”、array 换行拼接、object JSON.stringify。
- 再次测试 `POST /api/leads` 成功，返回 `{ success: true, reportId: "SCH-2026-TEST02" }`，说明 Leads 与 Scholarships 写入链路已打通。
- 当前验证：`npm run lint` 通过，`npm run build` 通过；本地 dev 服务运行在 `http://localhost:3005`。
- 安全提醒仍有效：用户曾明文发送 `FEISHU_APP_SECRET`，建议在飞书开放平台重置/轮换 Secret，并同步更新 `.env.local` 与 Vercel 环境变量。
- 用户随后重复给出“内部顾问小范围试用版”架构升级需求；经核验，当前代码已经包含所列核心文件、飞书封装、OpenRouter/Exa 骨架、文档、localStorage 降级和页面流程。重新执行 `npm run lint` 通过、`npm run build` 通过、`npm run dev` 可运行，本机端口占用后服务启动在 `http://localhost:3006`。
- 对本地 `http://localhost:3006` 做 smoke check：首页 `/`、测评页 `/assessment` 均返回 200；`/result` 无 localStorage 时返回 200 并展示空状态/引导逻辑，符合直接访问结果页的降级设计。
- 飞书新 Base 链接可用格式：`https://feishu.cn/base/HFD5bJChOaVYl8sWDegcaWMZnIA?table=tblBRlgBEVL5v1xq`；如需进入 Scholarships/AI Runs，可替换 table 参数为 `tblt8OVCvgm2gKXt` / `tblZFPveSOBPmS6d`。
- 用户询问 Exa 和 OpenRouter API 是否已跑通；检查本地 `.env.local` 发现 `OPENROUTER_API_KEY`、`OPENROUTER_MODEL_FAST`、`OPENROUTER_MODEL_STRONG`、`EXA_API_KEY` 均为空，尚未真实联调。用户选择“先配 API”。下一步需要用户把 OpenRouter/Exa key 与模型名配置到 `.env.local` 或 Vercel 环境变量，然后测试 `/api/admin/run-ai-review`。
- 用户在聊天中明文提供 OpenRouter API Key 和 Exa API Key，已写入本地 `.env.local`（被 `.gitignore` 忽略）。安全提醒：后续应建议用户在 OpenRouter / Exa 后台重置或轮换这些 key，并只保存在 `.env.local` 与 Vercel 环境变量中。
- OpenRouter/Exa 联调结果：OpenRouter API Key 可用，`nvidia/nemotron-3-nano-omni-30b-a3b-reasoning:free` 简单 JSON 调用成功；用户给的 `google/gemma-4-31b-it:free` 返回 429 upstream rate-limited，已将本地 `OPENROUTER_MODEL_STRONG` 临时改为同一个可用的 nemotron free 模型。Exa Search 与 Exa Contents 均返回 200，并能抓取 SOAS/Durham 等奖学金页面。
- `/api/admin/run-ai-review` 已真实跑通：能生成 query、调用 Exa 搜索和内容抓取、调用 OpenRouter 抽取/评分，返回 `success:true` 和候选奖学金；已增强降级逻辑：query 生成失败用规则 fallback，单个页面抽取/评分失败会跳过或使用抽取结果 fallback，完全抽取失败时基于 Exa 搜索结果返回待人工确认的 fallback candidates。
- AI 复核输出规范化：已修复 OpenRouter 返回数组 deadline、非枚举 sourceReliability 等导致类型/展示不稳定的问题；在 `app/api/admin/run-ai-review/route.ts` 中统一 normalize 文本、sourceType、sourceReliability，并限制一次最多处理 6 个 contents、返回最多 4 个候选以控制 free 模型耗时。
- 验证：`npm run lint` 通过；`npm run build` 通过。当前 dev 服务运行在 `http://localhost:3007`。
- 线上部署准备：已将内部顾问版与 AI 手动复核代码提交为 `ef95fd1 Add internal advisor AI review workflow.` 并推送到 GitHub `allinnb/JXJ-FOR-U` 的 `main` 分支；推送前 `npm run lint` 与 `npm run build` 均通过。
- Vercel 部署限制：当前环境没有 Vercel CLI（`vercel: command not found`），浏览器工具打开 Vercel Dashboard 失败（Chrome debugger 未启动），因此未能代用户直接写入 Vercel 环境变量；需要用户在 Vercel 项目 Settings → Environment Variables 手动配置 OpenRouter/Exa/飞书变量并 Redeploy。
- 线上新地址 `https://jxj-for-u-em47.vercel.app/` 已检测：首页 `/` 和测评页 `/assessment` 均返回 200，且包含新版首页标题、样例报告、3 步测评、微信选填提示等内容，说明前端新版已部署。
- 线上 `/result` 直接带旧 query 参数访问时仅返回引导/降级页，未渲染报告内容；这是当前设计导致：新版结果页主要依赖 localStorage，服务端 HTTP smoke test 无 localStorage。需要真实浏览器表单提交才能完整验证用户端报告页。
- 线上 `POST /api/leads` 返回 `{ success:false, error:"缺少服务端环境变量 FEISHU_BITABLE_APP_TOKEN" }`，说明 Vercel 当前部署缺少飞书环境变量或未在该 Deployment 生效；用户端仍能本地出报告，但飞书同步闭环未打通。
- 线上 `POST /api/admin/run-ai-review` 返回 `success:true`，能生成 fallback queries，调用 Exa/AI 复核链路并返回 GOV.UK Chevening、Commonwealth、Durham 等候选 fallback candidates；说明 OpenRouter/Exa 线上 AI 复核接口基本可执行。但因飞书环境变量缺失，AI Runs 写入飞书应未闭环。
- 最新用户提供的 Vercel 预览链接 `https://jxj-for-u-em47-o778qj5pt-allinnbtechgmailcoms-projects.vercel.app/` 已测试：首页、测评页和 `POST /api/leads` 均返回 Vercel Authentication Required（HTTP 401），说明该预览部署启用了 Vercel Deployment Protection / SSO 保护，外部自动测试无法访问应用或 API，因此无法判断飞书是否跑通。
- 需要用户提供一个未受保护的 Production 域名，或在 Vercel 关闭该 Deployment 的保护，或提供 Vercel Protection Bypass Token 后才能继续测试 `/api/leads` 和 AI/飞书闭环。
- 重新检测 `https://jxj-for-u-em47.vercel.app/`：首页 `/` 与测评页 `/assessment` 均返回 HTTP 200；线上 `POST /api/leads` 使用测试 reportId `SCH-2026-ONTEST` 返回 `{success:true}`，说明 Vercel 飞书 Leads/Scholarships 同步已经跑通；线上 `POST /api/admin/run-ai-review` 使用 `SCH-2026-AITEST2` 返回 `{success:true}`、生成 AIRUN 编号和候选奖学金，说明 OpenRouter/Exa AI 复核线上可执行，并且飞书环境变量已不再缺失。
- 全面产品优化轮次（P0–P3）：
  - P0: 给 `/api/admin/run-ai-review` 加 `x-admin-key` header 鉴权（可选，环境变量 `ADMIN_API_KEY` 不设置则不校验）；增加 OG 标签、favicon、viewport meta。
  - P1: 测评表单改为 3 步渐进式（进度条 + 上一步/下一步按钮 + 当前步骤校验），GPA 填写增加制式提示，经历字段增加 placeholder；结果页增加 urgency 提醒（报告时效性）、skeleton loading、版本号校验；埋点事件上报到 `/api/analytics` 服务端接口。
  - P2: 首页增加社交证据（156 位同学计数器 + 3 条模拟好评 + 顾问资质描述）；结果页奖学金卡片手机端默认折叠详情、点击展开；服务卡片增加价格锚点（¥99 划线原价 ¥199 + "热门"标签）。
  - P3: 新增 `/scholarships` 公开奖学金数据库页（SEO 友好）；新增 `/admin` 顾问后台页面（密码 `jxj2026`、飞书表快捷链接、AI 复核触发器、使用说明）；首页新增三大业务线导流区块（升学规划/奖学金申请/签证服务）。
  - 新增文件：`app/admin/page.tsx`、`app/api/analytics/route.ts`、`app/scholarships/page.tsx`、`public/favicon.svg`。
  - 修改文件：`app/api/admin/run-ai-review/route.ts`、`app/assessment/page.tsx`、`app/layout.tsx`、`app/page.tsx`、`components/PageShell.tsx`、`components/ResultClient.tsx`、`src/lib/analytics.ts`、`src/lib/config.ts`、`.env.example`、`tailwind.config.ts`。
  - 验证：`npm run lint` 通过、`npm run build` 通过（11 页全部生成）。提交 `e146b6e` 已推送到 GitHub。
  - Admin 密码 `jxj2012` 首版硬编码在客户端组件中，后续建议改为环境变量 + 服务端校验。
